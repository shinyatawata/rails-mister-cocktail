<h1>Add a dose</h1>
<%= simple_form_for([@cocktail, @dose]) do |f| %>
<%= f.association :ingredient %>
<%= f.input :description %>
<%= f.submit %>
<% end %>

<%= link_to "Go back", cocktail_path(@cocktail.id) %>
